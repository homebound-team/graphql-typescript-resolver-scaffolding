extend type Mutation {
  saveBook(input: BookInput!): SaveBookResult!
}

extend type Query {
  books: [Book!]!
}

type Book {
  name: String!
}

input BookInput {
  name: String
}

type SaveBookResult {
  author: Author!
}
